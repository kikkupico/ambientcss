# css-basic

A transport panel (Play / Stop / Record) built with plain HTML and
`@ambientcss/css` only — no framework, no build step.

Loads `@ambientcss/css` from jsDelivr and sets the light environment with
custom properties on a wrapper `<div>`.

## Run

Open `index.html` directly in a browser.
