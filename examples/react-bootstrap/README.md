# react-bootstrap

The same Play/Arm/Gain `AmbientPanel` as `react-basic`, laid out inside a
Bootstrap grid (`Container`/`Row`/`Col`/`Stack`) to show `@ambientcss/components`
sitting inside an existing Bootstrap layout.

Vite + React + TypeScript.

## Run

```bash
pnpm install
pnpm dev
```
