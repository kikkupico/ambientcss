# css-tailwind

A Play/Record panel showing `@ambientcss/css` alongside the Tailwind Play
CDN build — Tailwind handles layout and typography utilities, Ambient CSS
handles lighting, surfaces and edges on the same elements.

Both are loaded from CDN; no build step.

## Run

Open `index.html` directly in a browser.
