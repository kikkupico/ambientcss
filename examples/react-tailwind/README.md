# react-tailwind

The same Play/Arm/Gain `AmbientPanel` as `react-basic`, laid out with
Tailwind utility classes to show `@ambientcss/components` sitting inside a
Tailwind project.

Vite + React + TypeScript.

## Run

```bash
pnpm install
pnpm dev
```
