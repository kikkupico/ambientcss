# css-purecss

A Play/Stop panel showing `@ambientcss/css` layered over
[PureCSS](https://purecss.io/) — Ambient CSS classes handle lighting and
depth, PureCSS handles grid layout and button reset.

Both stylesheets are loaded from CDN; no build step.

## Run

Open `index.html` directly in a browser.
