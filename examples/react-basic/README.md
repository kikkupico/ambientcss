# react-basic

An `AmbientPanel` with a Play button, an Arm switch and a Gain knob —
`@ambientcss/components` on its own, styled with plain inline styles around
it (no CSS framework).

Vite + React + TypeScript.

## Run

```bash
pnpm install
pnpm dev
```
